name: muebleria_bi
version: 1.0.0
config-version: 2

profile: muebleria_bi

model-paths:
  - models
analysis-paths:
  - analyses
test-paths:
  - tests
seed-paths:
  - seeds
macro-paths:
  - macros
snapshot-paths:
  - snapshots

clean-targets:
  - target
  - dbt_packages

models:
  muebleria_bi:
    staging:
      +materialized: view
      +schema: staging
      +tags:
        - staging
    marts:
      +materialized: table
      +schema: marts
      +tags:
        - marts
